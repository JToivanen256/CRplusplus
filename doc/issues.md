Known issues:

- On some devices (maybe newer CPUs?) need to run "export LIBGL_ALWAYS_SOFTWARE=1" in the root to make SFML even show the game

- No sounds

- Dumb sprites

- No animations

- Pathfinding breaks if path doesn't exist (bad map then)